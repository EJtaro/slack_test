{
  "name": "slack-anonymous",
  "version": "1.0.0",
  "description": "Slack App",
  "main": "index.js",
  "scripts": {
    "dev": "netlify-lambda serve resources/api --port 3326",
    "build": "netlify-lambda build resources/api --port 3326",
    "build2": "node --openssl-legacy-provider npm run build"
  },
  "dependencies": {
    "netlify-lambda": "^2.0.10",
    "query-string": "7.0.1",
    "@slack/web-api": "^6.2.4"
  }
}
